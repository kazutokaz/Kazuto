services:
  api:
    image: python:3.11-slim
    working_dir: /workspace/generated_app
    volumes:
      - ./workspace:/workspace
    ports:
      - "8001:8000"
    command: bash -lc "pip install fastapi uvicorn && uvicorn app.main:app --host 0.0.0.0 --port 8000"
